# server
PORT=8080
NODE_ENV=development

# auth
JWT_SECRET=dev-secret-change-in-prod

#cors
CORS_ORIGIN=http://localhost:3000,http://localhost:8080

# Database (for local migrations - uses localhost)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/myexpressdb"

DB_PASSWORD=postgres
DB_PORT=5432
POSTGRES_DB=myexpressdb

# Prisma Engine (optional)
PRISMA_CLIENT_ENGINE_TYPE=binary
# security

# rate limiting
